image: docker:latest

stages:
  - init

variables:
  GIT_STRATEGY: none

init-project:
  stage: init
  image: dokcer:latest
  script:
    - pwd
    - mkdir /home/gitlab-runner/projects
    - cd /home/gitlab-runner/projects
    - git clone ssh://git@gitlab.farakaft.ir/h.azarbad/similarity-bot.git
    - cd similarity-bot
    - docker-compose up -d